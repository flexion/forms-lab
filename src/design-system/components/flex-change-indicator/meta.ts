import type { ComponentMeta } from '../../types'

export const meta: ComponentMeta = {
  name: 'Change Indicator',
  slug: 'flex-change-indicator',
  category: 'feedback',
  description:
    'Small colored dot (with optional label) marking an item as modified, added, or removed.',
  kind: 'custom',
  interactive: false,
}
